---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: go2rtc
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: go2rtc-secret
    template:
      data:
        go2rtc.yaml: |
            log:
              level: debug
            api:
              listen: :80
              origin: "*"
            rtsp:
              listen: :554
            webrtc:
              listen: :8555
            streams:
              kaiser-cam-01:
                - ffmpeg:rtsp://scrypted.home.svc.cluster.local:38345/7989ec270eda9861#video=copy#audio=copy#audio=aac
            publish:
              kaiser-cam-01:
                - rtmps://a.rtmp.youtube.com/live2/{{ .Kaiser_Newt_01 }}
  dataFrom:
    - extract:
        key: youtube-keys