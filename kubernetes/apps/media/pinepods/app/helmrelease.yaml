---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pinepods
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: pinepods
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      pinepods:
        annotations:
          reloader.stakater.com/auto: "true"
        labels:
          nfsMount: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 17
            envFrom: &envFrom
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
        containers:
          app:
            image:
              repository: madeofpendletonwool/pinepods
              tag: 0.7.9@sha256:d722388502248c91e154377a8ac5299a2b0b4becda65d15cc7c00feee675a355
            env:
              PUID: 2000
              PGID: 2000
              TZ: America/Chicago
              HOSTNAME: https://pods.jonandlinz.io
              SEARCH_API_URL: https://search.pinepods.online/api/search
              PEOPLE_API_URL: https://people.pinepods.online
              # Valkey
              VALKEY_HOST: dragonfly.database.svc.cluster.local
              VALKEY_PORT: 6379
            envFrom: *envFrom
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  failureThreshold: 30
                  periodSeconds: 5
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 1Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 2000
        runAsGroup: 2000
        fsGroup: 2000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: &port 8040
    route:
      app:
        hostnames:
          - "pods.jonandlinz.io"
        parentRefs:
          - name: envoy-external
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      cache:
        type: emptyDir
        globalMounts:
          - path: /pinepods/cache
      config:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /opt/pinepods
      media:
        type: nfs
        server: nas.jonandlinz.com
        path: /mnt/rustpool/media_server/media/podcasts
        globalMounts:
          - path: /opt/pinepods/downloads
      tmpfs:
        type: emptyDir
        advancedMounts:
          pinepods:
            app:
              - path: /tmp
                subPath: tmp