---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/probe_v1.json
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: lan-icmp
spec:
  module: icmp
  prober:
    url: blackbox-exporter-lan.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - nas.jonandlinz.com
        - pikvm.jonandlinz.com
        - ui.jonandlinz.com
        - zigbee-controller.jonandlinz.com
        - zwave-controller.jonandlinz.com
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/probe_v1.json
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: lan-tcp
spec:
  module: tcp_connect
  prober:
    url: blackbox-exporter-lan.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - nas.jonandlinz.com:2049